abi <abi/5.0>,

include <tunables/global>

profile alsamixer /{usr,}/bin/alsamixer flags=(attach_disconnected.path=/att/alsamixer/) {
    include <abstractions/base>
    include <abstractions/consoles>
    include <abstractions/nameservice-strict>
    include <abstractions/terminfo>

    include <abstractions/dbus-session-strict>

    @{exec_path} mr,

    @{sys}/devices/virtual/dmi/id/sys_vendor r,

    @{PROC}/@{pid}/task/@{tid}/comm rw,

    # pipewire configs
    /usr/share/pipewire/*.conf r,
    /usr/share/pipewire/client.conf.d/{,*.conf} r,
    # pulseaudio configs
    @{etc_ro}/pulse/*.conf r,
    @{etc_ro}/pulse/*.conf.d/ r,
    @{etc_ro}/pulse/*.conf.d/*.conf r,
    # alsa configs
    /usr/share/alsa/*.conf r,
    /usr/share/alsa/**/*.conf r,
    @{etc_ro}/alsa/conf.d/ r,
    @{etc_ro}/alsa/conf.d/*.conf r,
    @{etc_ro}/alsa/conf.d/*.conf.example r,
    @{etc_ro}/asound.conf r,
    owner @{HOME}/.asoundrc r,
    # alsa info files
    @{PROC}/asound/** r,

    # openSUSE-Leap 15.6 version needs to ls /dev/shm for some reason
    /dev/shm/ r,
    # /dev/snd devices
    /dev/snd/controlC[0-9]* rw,
    # Folders contain symlinks to device files in /dev/snd
    /dev/snd/by-id/ r,
    /dev/snd/by-path/ r,

    @{sys}/devices/virtual/dmi/id/{bios_vendor,board_vendor,product_name} r,

    # PulseAudio communication channels
    owner @{HOME}/.config/pulse/cookie rk,
    owner /run/user/@{uid}/pulse/ r,

    # Pipewire communication channels
    owner /run/user/@{uid}/pipewire-[0-9] rw,

    /att/alsamixer/systemd/journal/socket w,

    include if exists <local/alsamixer>
}
