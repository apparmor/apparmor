#
#=Description test that := can be embedded in list element when embedded in quotes
#=EXRESULT PASS

@{ONE}=one
@{TWO}=two
@{THREE}=three

/usr/bin/foo {
  mount fstype=nfs{,4} "*:=**" -> /var/snap/{@{ONE},@{TWO}}/@{THREE}/{,**},
}
